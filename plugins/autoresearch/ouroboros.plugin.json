{
  "schema_version": "0.1",
  "name": "autoresearch",
  "version": "0.1.0",
  "description": "Ouroboros integration for Karpathy-style autoresearch experiment loops.",
  "source": {
    "type": "local_path",
    "path": "plugins/autoresearch"
  },
  "commands": [
    {
      "namespace": "auto-research",
      "name": "inspect",
      "summary": "Inspect an autoresearch checkout and report its Ouroboros handoff readiness.",
      "usage": "ooo auto-research inspect <repository-path>",
      "risk": "read_only",
      "requires_confirmation": false,
      "arguments": [
        {
          "name": "repository_path",
          "type": "path",
          "required": true,
          "description": "Path to a local autoresearch-compatible repository."
        },
        {
          "name": "program_file",
          "type": "path",
          "required": false,
          "description": "Repository-relative path to the autoresearch program brief (default: program.md)."
        },
        {
          "name": "target_file",
          "type": "path",
          "required": false,
          "description": "Repository-relative path to the editable training target (default: train.py)."
        },
        {
          "name": "support_file",
          "type": "path",
          "required": false,
          "description": "Repository-relative path to the fixed support/preparation file (default: prepare.py)."
        }
      ]
    },
    {
      "namespace": "auto-research",
      "name": "prepare",
      "summary": "Generate an Ouroboros Seed handoff for a bounded autoresearch loop.",
      "usage": "ooo auto-research prepare <repository-path> --goal <goal>",
      "risk": "write",
      "requires_confirmation": false,
      "arguments": [
        {
          "name": "repository_path",
          "type": "path",
          "required": true,
          "description": "Path to a local autoresearch-compatible repository."
        },
        {
          "name": "goal",
          "type": "string",
          "required": true,
          "description": "Research objective to pass into ooo auto as the Seed goal."
        },
        {
          "name": "program_file",
          "type": "path",
          "required": false,
          "description": "Repository-relative path to the autoresearch program brief (default: program.md)."
        },
        {
          "name": "target_file",
          "type": "path",
          "required": false,
          "description": "Repository-relative path to the editable training target (default: train.py)."
        },
        {
          "name": "support_file",
          "type": "path",
          "required": false,
          "description": "Repository-relative path to the fixed support/preparation file (default: prepare.py)."
        },
        {
          "name": "metric",
          "type": "string",
          "required": false,
          "description": "Primary comparison metric to optimize and report (default: val_bpb)."
        },
        {
          "name": "max_experiments",
          "type": "integer",
          "required": false,
          "description": "Maximum number of experiments to include in the handoff budget (default: 8)."
        },
        {
          "name": "experiment_seconds",
          "type": "integer",
          "required": false,
          "description": "Per-experiment wall-clock budget in seconds (default: 300)."
        },
        {
          "name": "train_command",
          "type": "string",
          "required": false,
          "description": "Verification command agents should run for each experiment (default: uv run train.py)."
        }
      ]
    }
  ],
  "capabilities": [
    {
      "name": "seed",
      "access": "write",
      "reason": "Prepare a Seed-shaped Markdown brief that ooo auto can consume from the generated handoff."
    },
    {
      "name": "ledger",
      "access": "write",
      "reason": "Preserve experiment constraints and handoff evidence in files that can be attached to the Ouroboros ledger by the caller."
    },
    {
      "name": "provenance",
      "access": "write",
      "reason": "Record checkout and file evidence inside handoff.json for later Ouroboros provenance attachment."
    },
    {
      "name": "state",
      "access": "write",
      "reason": "Persist prepared handoff artifacts under the target repository."
    },
    {
      "name": "handoff",
      "access": "attach",
      "reason": "Prepare seed.md, auto_goal.txt, and handoff.json as explicit handoff artifacts for ooo auto."
    },
    {
      "name": "progress",
      "access": "write",
      "reason": "Report readiness and missing prerequisites as structured JSON before ooo auto starts."
    }
  ],
  "permissions": [
    {
      "scope": "filesystem:read",
      "risk": "read_only",
      "required": true,
      "reason": "Inspect program.md, prepare.py, train.py, and repository layout."
    },
    {
      "scope": "filesystem:write",
      "risk": "write",
      "required": true,
      "reason": "Write the generated Seed and handoff manifest into .ouroboros/autoresearch/."
    }
  ],
  "entrypoint": {
    "type": "command",
    "command": "python -m ouroboros_autoresearch"
  },
  "audit": {
    "events": [
      "plugin.invoked",
      "plugin.permission_used",
      "plugin.completed",
      "plugin.failed"
    ]
  }
}
