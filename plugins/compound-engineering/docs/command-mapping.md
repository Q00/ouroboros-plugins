# Compound Engineering command mapping

Upstream: `EveryInc/compound-engineering-plugin` version `3.8.3`.

Every upstream skill is exposed under the schema-valid `compound` namespace. The original `ce-*` relationship is preserved in the upstream-skill column.

| Upstream skill | AgentOS/Ouroboros command | Risk | Permissions | Artifacts | Handoff |
|---|---|---|---|---|---|
| `ce-agent-native-architecture` | `ooo compound agent-native-architecture` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/agent-native-architecture/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-agent-native-audit` | `ooo compound agent-native-audit` | `read_only` | `filesystem:read` | `.omx/compound/agent-native-audit/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-brainstorm` | `ooo compound brainstorm` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/brainstorms/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-clean-gone-branches` | `ooo compound clean-gone-branches` | `destructive` | `filesystem:read, filesystem:write, git:write, shell:execute` | `.omx/compound/clean-gone-branches/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-code-review` | `ooo compound code-review` | `read_only` | `filesystem:read` | `.omx/compound/reviews/<timestamp>-<scope>.json; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-commit` | `ooo compound commit` | `write` | `filesystem:read, filesystem:write, git:write, shell:execute` | `.omx/compound/commit/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-commit-push-pr` | `ooo compound commit-push-pr` | `destructive` | `filesystem:read, filesystem:write, git:write, github:pull_request:write, network:write, shell:execute` | `.omx/compound/commit-push-pr/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-compound` | `ooo compound compound` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/learnings/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-compound-refresh` | `ooo compound compound-refresh` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/learnings/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-debug` | `ooo compound debug` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/debug/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-demo-reel` | `ooo compound demo-reel` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/demo-reel/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-dhh-rails-style` | `ooo compound dhh-rails-style` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/dhh-rails-style/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-doc-review` | `ooo compound doc-review` | `read_only` | `filesystem:read` | `.omx/compound/doc-review/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-frontend-design` | `ooo compound frontend-design` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/frontend-design/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-gemini-imagegen` | `ooo compound gemini-imagegen` | `write` | `filesystem:read, filesystem:write, network:write, api_key:gemini` | `.omx/compound/gemini-imagegen/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-ideate` | `ooo compound ideate` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/ideate/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-optimize` | `ooo compound optimize` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/optimize/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-plan` | `ooo compound plan` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/plans/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-polish-beta` | `ooo compound polish-beta` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/polish-beta/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-product-pulse` | `ooo compound product-pulse` | `read_only` | `filesystem:read` | `.omx/compound/product-pulse/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-proof` | `ooo compound proof` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/proof/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-release-notes` | `ooo compound release-notes` | `read_only` | `filesystem:read` | `.omx/compound/release-notes/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-report-bug` | `ooo compound report-bug` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/report-bug/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-resolve-pr-feedback` | `ooo compound resolve-pr-feedback` | `destructive` | `filesystem:read, filesystem:write, github:pull_request:write, network:write, git:write` | `.omx/compound/resolve-pr-feedback/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-riffrec-feedback-analysis` | `ooo compound riffrec-feedback-analysis` | `read_only` | `filesystem:read` | `.omx/compound/riffrec-feedback-analysis/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-sessions` | `ooo compound sessions` | `read_only` | `filesystem:read` | `.omx/compound/sessions/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-setup` | `ooo compound setup` | `write` | `filesystem:read, filesystem:write, shell:execute` | `.omx/compound/setup/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-simplify-code` | `ooo compound simplify-code` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/simplify-code/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-slack-research` | `ooo compound slack-research` | `write` | `filesystem:read, filesystem:write, slack:read, network:read` | `.omx/compound/slack-research/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-strategy` | `ooo compound strategy` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/strategy/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-test-browser` | `ooo compound test-browser` | `write` | `filesystem:read, filesystem:write, browser:automation, shell:execute` | `.omx/compound/test-browser/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-test-xcode` | `ooo compound test-xcode` | `write` | `filesystem:read, filesystem:write, mcp:call, xcode:execute, shell:execute` | `.omx/compound/test-xcode/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-update` | `ooo compound update` | `write` | `filesystem:read, filesystem:write, network:read` | `.omx/compound/update/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-work` | `ooo compound work` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/work/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-work-beta` | `ooo compound work-beta` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/work-beta/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `ce-worktree` | `ooo compound worktree` | `write` | `filesystem:read, filesystem:write` | `.omx/compound/worktree/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
| `lfg` | `ooo compound lfg` | `destructive` | `filesystem:read, filesystem:write, shell:execute, git:write, github:pull_request:write, network:write` | `.omx/compound/lfg/<slug>.md; .omx/compound/runs/<run-id>/result.json` | Loads the vendored upstream skill instructions and emits a bounded AgentOS handoff artifact; execution remains under Ouroboros runtime permissions. |
